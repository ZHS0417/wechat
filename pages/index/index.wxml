<!--index.wxml-->
<view class="map-con">
  <map id="map" class="map" longitude="{{longitude}}" latitude="{{latitude}}" controls="{{controls}}" bindcontroltap="controltap" markers="{{markers}}" bindmarkertap="markertap" scale="16" show-location style="width: 100%; height:100%;"></map>
</view>
<view class="bottom-area">
  <navigator url="/pages/publish/publish" class="publish">发布</navigator>
  <view class="search">搜索</view>
</view>